# Тестирование удаления данных и повторного онбординга

## ✅ Как это работает

### Процесс удаления:
1. Пользователь отправляет `/delete_data`
2. Бот показывает подтверждение с предупреждением
3. Пользователь подтверждает удаление
4. **Удаляется ВСЁ:**
   - Пользователь (User)
   - Все моменты (Moments)
   - Вся история диалогов (Conversations)
   - Статистика (UserStats)
   - Запланированные уведомления (ScheduledNotification)

### После удаления:
- Пользователь **полностью удален** из базы данных
- При следующем `/start` создается **новый пользователь** с дефолтными значениями
- `onboarding_completed = False` (дефолт)
- Пользователь попадает в **новый онбординг**

## 🧪 Сценарий тестирования

### Шаг 1: Проверка текущего состояния
1. Отправь `/start` боту
2. **Ожидаемо:** Если уже прошел онбординг → "С возвращением!"
3. Проверь, что есть данные:
   - Зайди в `/moments` - должны быть моменты (если есть)
   - Зайди в `/stats` - должна быть статистика
   - Зайди в `/settings` - должны быть настройки

### Шаг 2: Удаление данных
1. Отправь команду `/delete_data`
2. **Ожидаемо:** Появится сообщение с предупреждением:
   ```
   ⚠️ Удаление данных
   
   Ты уверен, что хочешь удалить ВСЕ свои данные?
   
   Это действие:
   • Удалит все твои моменты
   • Удалит историю диалогов
   • Удалит статистику
   • Сбросит настройки
   
   ⚠️ Это действие необратимо!
   ```
3. Нажми кнопку подтверждения (обычно "Да, удалить" или "Подтвердить")
4. **Ожидаемо:** Сообщение об успешном удалении данных

### Шаг 3: Проверка удаления
1. Попробуй отправить `/moments`
2. **Ожидаемо:** Ошибка или сообщение, что пользователь не найден (так как пользователь удален)

### Шаг 4: Повторный вход как новый пользователь
1. Отправь `/start` боту
2. **Ожидаемо:** 
   - Приветственное сообщение с информацией о голосовых сообщениях
   - Кнопки выбора формы обращения (ты/вы)
   - **НЕ** должно быть "С возвращением!"

### Шаг 5: Прохождение нового онбординга
1. Выбери форму обращения (например, "На «ты»")
2. **Ожидаемо:** Появится выбор пола (мужчина/женщина/нейтрально)
3. Выбери пол
4. **Ожидаемо:** Появится объяснение важности часового пояса
5. Выбери часовой пояс
6. **Ожидаемо:** 
   - Финальное сообщение с инструкциями
   - Первый вопрос от бота
   - Онбординг завершен

## 🔍 Что проверить

### Критичные проверки:
- [ ] После удаления пользователь **полностью удален** из базы
- [ ] При `/start` создается **новый пользователь** (не обновляется старый)
- [ ] Новый пользователь имеет `onboarding_completed = False`
- [ ] Пользователь попадает в **новый онбординг** (не в "С возвращением!")
- [ ] Все шаги нового онбординга работают:
  - [ ] Приветствие с информацией о голосовых
  - [ ] Выбор формы обращения
  - [ ] Выбор пола (с опцией "нейтрально")
  - [ ] Обязательный выбор часового пояса
  - [ ] Финальное сообщение с примерами
  - [ ] Первый вопрос отправляется

### Проверка данных:
- [ ] После удаления `/moments` не показывает старые моменты
- [ ] После удаления `/stats` не показывает старую статистику
- [ ] После удаления настройки сброшены на дефолты

## 📝 Логи для отладки

Проверь логи при удалении:
```
Deleting all data for user {telegram_id}
Deleted scheduled notifications for user {user_id}
Deleted stats for user {user_id}
Deleted conversations for user {user_id}
Deleted moments for user {user_id}
Successfully deleted all data for user {telegram_id}
```

Проверь логи при повторном `/start`:
```
Created new user: {telegram_id} (gender: {detected_gender})
```

## ⚠️ Важные замечания

1. **Удаление необратимо** - все данные удаляются навсегда
2. **Новый пользователь** - это не тот же пользователь, это новый запись в базе
3. **ID пользователя** - `id` в базе будет новый, но `telegram_id` останется тот же
4. **Статистика** - вся статистика (streak, moments count) начнется с нуля

## 🐛 Возможные проблемы

### Проблема: После удаления `/start` показывает "С возвращением!"
**Причина:** Пользователь не был полностью удален или есть кеш
**Решение:** 
- Проверь логи - должно быть "Successfully deleted all data"
- Проверь базу данных напрямую
- Убедись, что `get_or_create_user` создает нового пользователя, а не находит старого

### Проблема: Онбординг не запускается после удаления
**Причина:** `onboarding_completed` не сброшен на `False`
**Решение:**
- Проверь модель User - `onboarding_completed` должен иметь `default=False`
- Проверь, что при создании нового пользователя не устанавливается `onboarding_completed=True`

### Проблема: Старые данные остались
**Причина:** Удаление не сработало полностью
**Решение:**
- Проверь логи удаления
- Проверь базу данных напрямую
- Убедись, что все связанные таблицы удалены (CASCADE)

## 🔄 Альтернативный способ (для разработки)

Если нужно быстро сбросить онбординг без полного удаления данных:

1. Можно добавить команду `/reset_onboarding` (если нужно)
2. Или напрямую в базе: `UPDATE users SET onboarding_completed = FALSE WHERE telegram_id = {your_id}`

Но для полного тестирования лучше использовать `/delete_data`.
