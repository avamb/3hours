# MINDSETHAPPYBOT 🌟

Telegram-бот для развития позитивного мышления. Каждые 3 часа (в настраиваемые активные часы) спрашивает пользователя "Что хорошего произошло?", сохраняет ответы в векторную базу данных и использует историю для персонализированной поддержки.

## ✨ Возможности

- 📝 **Записывайте хорошие моменты** — текстом или голосом
- 🔔 **Умные напоминания** — в удобное для вас время
- 💝 **Персонализированная поддержка** — бот напомнит о прошлых радостях, когда вам грустно
- 📊 **Статистика и стрики** — отслеживайте свой прогресс
- 💬 **Свободный диалог** — поговорите с ботом, когда нужна поддержка
- 🔒 **GDPR-совместимость** — полный контроль над своими данными

## 🛠 Технологии

- **Python 3.11+** + **aiogram 3.x** — Telegram Bot API
- **PostgreSQL 15+** + **pgvector** — векторная база данных
- **SQLAlchemy 2.x** + **asyncpg** — асинхронный ORM
- **OpenAI GPT-4 & Whisper** — AI-ответы и распознавание речи
- **APScheduler** — планировщик уведомлений
- **Docker** — контейнеризация

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.11+
- PostgreSQL 15+ с расширением pgvector
- Telegram Bot Token (от [@BotFather](https://t.me/BotFather))
- OpenAI API Key

### Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd mindsethappybot
   ```

2. **Создайте файл `.env`:**
   ```bash
   cp .env.example .env
   # Отредактируйте .env и добавьте свои ключи
   ```

3. **Запустите через init.sh:**
   ```bash
   chmod +x init.sh
   ./init.sh
   ```

   Или с Docker:
   ```bash
   ./init.sh 1  # Полная установка с Docker
   ```

### Конфигурация

Создайте файл `.env` со следующими переменными:

```env
# Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here

# OpenAI
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_CHAT_MODEL=gpt-4o
OPENAI_ANALYSIS_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Database
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/mindsethappybot
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=mindsethappybot

# Optional
LOG_LEVEL=INFO
DEFAULT_TIMEZONE=UTC
```

## 📱 Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Начать работу с ботом |
| `/help` | Показать справку |
| `/moments` | Просмотреть историю моментов |
| `/stats` | Посмотреть статистику |
| `/settings` | Открыть настройки |
| `/talk` | Начать свободный диалог |
| `/privacy` | Политика конфиденциальности |
| `/export_data` | Экспортировать свои данные |
| `/delete_data` | Удалить все свои данные |

## 🔧 Разработка

### Структура проекта

```
mindsethappybot/
├── src/
│   ├── bot/
│   │   ├── handlers/     # Обработчики команд и сообщений
│   │   ├── keyboards/    # Клавиатуры (reply и inline)
│   │   ├── middlewares/  # Middleware (логирование и др.)
│   │   └── main.py       # Точка входа бота
│   ├── db/
│   │   ├── models/       # SQLAlchemy модели
│   │   ├── repositories/ # Data Access Layer
│   │   └── database.py   # Подключение к БД
│   ├── services/         # Бизнес-логика
│   └── config.py         # Конфигурация
├── alembic/              # Миграции БД
├── tests/                # Тесты
├── docker/               # Docker-файлы
├── init.sh               # Скрипт инициализации
└── requirements.txt      # Python зависимости
```

### Запуск тестов

```bash
pytest tests/ -v
```

### Регрессионные тесты (Node.js)

Регрессионные сценарии (`test-*.mjs`) хранятся в `tests/regression/` (не в корне репозитория).

Пример запуска:

```bash
node tests/regression/test-api.mjs
```

### Миграции базы данных

```bash
# Создать новую миграцию
alembic revision --autogenerate -m "Description"

# Применить миграции
alembic upgrade head

# Откатить миграцию
alembic downgrade -1
```

## 📊 База данных

### Таблицы

- **users** — пользователи и их настройки
- **moments** — записанные хорошие моменты с векторными эмбеддингами
- **conversations** — история диалогов для контекста
- **user_stats** — статистика и стрики
- **scheduled_notifications** — запланированные уведомления
- **question_templates** — шаблоны вопросов

## 🔒 Безопасность и приватность

- Данные используются **только** для персонализации общения с конкретным пользователем
- Полное соответствие GDPR и CCPA
- Пользователь может **экспортировать** или **удалить** все свои данные в любой момент
- При первом запуске показывается информация о хранении данных

## 🤝 Участие в разработке

Мы приветствуем pull requests! Пожалуйста:

1. Форкните репозиторий
2. Создайте ветку для фичи (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект лицензирован под MIT License.

---

Сделано с 💝 для развития позитивного мышления
