# 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ ПО МИГРАЦИИ НА TIMESTAMPTZ

## ⚠️ ВАЖНО
Эта миграция изменяет тип всех datetime колонок в базе данных. Выполняйте строго по инструкции!

## 📅 Когда проводить миграцию
- В период низкой активности пользователей
- После уведомления команды
- Иметь 2 часа времени на весь процесс

## 🔧 Подготовка

### 1. Проверка текущего состояния
```bash
# Проверить текущую миграцию
cd /path/to/3hours
alembic current

# Должно показать: 0016 (или 0017 если уже мигрировали)
```

### 2. Остановка бота
```bash
# Остановить Docker контейнеры
docker-compose stop bot
docker-compose stop admin

# Или если используете systemd
sudo systemctl stop mindsethappybot
```

## 📦 Резервное копирование

### 3. Создание backup (ОБЯЗАТЕЛЬНО!)
```bash
# Дать права на выполнение
chmod +x scripts/backup_before_migration.sh

# Запустить backup
./scripts/backup_before_migration.sh

# Проверить что backup создан
ls -la backups/
```

## 🚀 Применение миграции

### 4. Запуск миграции
```bash
# Применить миграцию
alembic upgrade head

# Если нужно откатить:
# alembic downgrade -1
```

### 5. Проверка миграции
```bash
# Запустить скрипт проверки
python scripts/verify_migration.py

# Должен показать:
# ✅ MIGRATION SUCCESSFUL!
# ✅ APPLICATION COMPATIBILITY: PASSED
```

## 🔄 Обновление кода

### 6. Обновление моделей и сервисов
```bash
# Автоматическое обновление всех файлов
python scripts/update_models_for_timestamptz.py

# Проверить изменения
git diff src/
```

### 7. Тестирование
```bash
# Запустить бота в тестовом режиме
python -m src.bot.main

# Проверить основные функции:
# - Создание момента
# - Просмотр моментов (Today/Week/Month)
# - Отправка уведомлений
# - Диалоги
```

## ✅ Финализация

### 8. Коммит изменений
```bash
# Добавить изменения
git add -A
git commit -m "feat: migrate to TIMESTAMPTZ for proper timezone handling

- Applied migration 0017
- Updated all models to use DateTime(timezone=True)
- Removed all .replace(tzinfo=None) workarounds
- Database now properly handles timezones"

# Запушить
git push origin refactor/code-review-fixes
```

### 9. Запуск в production
```bash
# Запустить контейнеры
docker-compose up -d

# Или systemd
sudo systemctl start mindsethappybot

# Проверить логи
docker-compose logs -f bot
```

## 🔴 Откат (если что-то пошло не так)

### Вариант 1: Откат миграции
```bash
# Остановить бота
docker-compose stop bot

# Откатить миграцию
alembic downgrade -1

# Восстановить старый код
git checkout -- src/

# Запустить бота
docker-compose up -d bot
```

### Вариант 2: Восстановление из backup
```bash
# Найти последний backup
ls -la backups/

# Восстановить базу
gunzip < backups/full_backup_YYYYMMDD_HHMMSS.sql.gz | \
  psql -h localhost -U postgres -d mindsethappybot

# Запустить бота со старым кодом
git checkout HEAD~1
docker-compose up -d bot
```

## 📊 Проверка после миграции

### 10. Мониторинг (первые 24 часа)
- [ ] Нет ошибок в логах связанных с timezone
- [ ] Уведомления приходят в правильное время
- [ ] Фильтры Today/Week/Month работают корректно
- [ ] Новые моменты сохраняются правильно
- [ ] Статистика отображается корректно

### 11. Финальная проверка
```sql
-- Проверить типы колонок в psql
\d+ users
\d+ moments
\d+ conversations

-- Все datetime колонки должны быть:
-- timestamp with time zone
```

## 📝 Чек-лист

### До миграции:
- [ ] Уведомил команду о maintenance
- [ ] Создал backup базы данных
- [ ] Остановил бота
- [ ] Проверил место на диске для backup

### Миграция:
- [ ] Применил alembic миграцию
- [ ] Запустил verify_migration.py
- [ ] Обновил код через update_models_for_timestamptz.py
- [ ] Протестировал основные функции

### После миграции:
- [ ] Бот запущен и работает
- [ ] Нет ошибок в логах
- [ ] Уведомления работают
- [ ] Создал и проверил тестовый момент
- [ ] Удалил временные .bak файлы

## ❓ FAQ

**Q: Сколько времени займет миграция?**
A: Сама миграция ~5-10 минут, весь процесс с проверками ~1-2 часа

**Q: Можно ли откатить миграцию?**
A: Да, через `alembic downgrade -1` или восстановление из backup

**Q: Что делать если verify_migration.py показывает ошибки?**
A: Не запускать бота, откатить миграцию, проверить логи

**Q: Нужно ли менять код после миграции?**
A: Да, запустите update_models_for_timestamptz.py для автоматического обновления

## 🆘 Поддержка

Если возникли проблемы:
1. Сохраните логи ошибок
2. Откатите миграцию
3. Свяжитесь с разработчиком

---
*Последнее обновление: 28.01.2026*